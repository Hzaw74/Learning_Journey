#include <stdio.h>

int main (void)
{
    int num = 0;
    printf("This is a guessing game where the number is between 0 and 100.\n");
    printf("Enter a number: ");
    scanf("%d", &num);

    if (num < 50)
    {
        num = num 
    }
}