#include <stdio.h>

int main (void)
{
    int subject;
    int grade;
    int total;

    printf("How many subjects: ");
    scanf("%d, &subject");

    for (int i = 0; i < subject; i++)
    {
        grade = cal_grade (int x, int y, int z);
    }

    total = total_grade();

}

int cal_grade (int x, int y, int z)
{
    for (j = 1; j < subject; j++)
    {
        
    }
    printf("Assignment: %d", &x);
    printf("Quiz: %d", &y);
    printf("Exam: %d", &z);

    return (x + y + z);
}

int total_grade (int a)
{
    return ()
}