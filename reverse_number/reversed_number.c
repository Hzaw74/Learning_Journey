#include <stdio.h>

int main (void)
{
    int num;
    printf("Enter a list of numbers: ");
    scanf("%d", &num);

    int count;
    if (num != 0)
    {
        while (int y = num; )
        {
            num/=;
            
        }
        
    }
    else
    {

    }
}